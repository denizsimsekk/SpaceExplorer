CREATE TABLE SpaceLaunchesCache (
    cache_key TEXT PRIMARY KEY NOT NULL,
    json_data TEXT NOT NULL
);

insertOrReplace:
INSERT OR REPLACE INTO SpaceLaunchesCache(cache_key, json_data)
VALUES (?, ?);

getCache:
SELECT json_data FROM SpaceLaunchesCache WHERE cache_key = ?;
